let x = 1

let y = 10

for (let index = x; index <= y; index++) {
    game.splash("counting to 10: " + index)
}
let d = 11

let p = 20

for (let index = d; index <= p; index++) {
    game.splash("counting to 20: " + index)   
}
let a = 5

let b = 0

for (let index = a; index >= b; index--) {
    game.splash("counting down to 0: " + index)
}
let g = 100

let h = 0

for (let index = g; index >= h; index-=10) {
    game.splash("counting to 0 by 10: " + index)
}
